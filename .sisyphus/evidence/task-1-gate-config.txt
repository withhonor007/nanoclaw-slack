TASK-1: Gate Config Validation
Generated: 2026-02-25
Plan: slack-round4-recovery-canary-testing

=== RECOVERY_EXHAUSTED_GATE_MS Configuration ===

File: src/config.ts
Lines: 59-68

Source (verbatim):
  const recoveryExhaustedGateMsRaw =
    process.env.RECOVERY_EXHAUSTED_GATE_MS ||
    envConfig.RECOVERY_EXHAUSTED_GATE_MS ||
    '0';
  const parsedRecoveryExhaustedGateMs = parseInt(recoveryExhaustedGateMsRaw, 10);
  export const RECOVERY_EXHAUSTED_GATE_MS =
    Number.isFinite(parsedRecoveryExhaustedGateMs) && parsedRecoveryExhaustedGateMs >= 0
      ? parsedRecoveryExhaustedGateMs
      : 0;

=== Validation Results ===

| Check                                      | Result |
|--------------------------------------------|--------|
| Env var name: RECOVERY_EXHAUSTED_GATE_MS   | PASS   |
| Fallback chain: env → envConfig → '0'      | PASS   |
| Default value when unset: 0               | PASS   |
| Safe parseInt with radix 10               | PASS   |
| Guard: non-finite → 0                     | PASS   |
| Guard: negative → 0                       | PASS   |
| Export name matches assumption A4          | PASS   |

=== Behavior Summary ===
- When RECOVERY_EXHAUSTED_GATE_MS is unset: exports 0 (gate disabled, all exhaustion events pass)
- When set to valid positive integer: exports that value (gate active, filters by time window)
- When set to invalid/negative: exports 0 (safe fallback)
- This means canary can observe exhaustion_drop events without any gate filtering by default.

=== Env var also registered in config validation list ===
src/config.ts line 12: 'RECOVERY_EXHAUSTED_GATE_MS' is in the known env vars array.
