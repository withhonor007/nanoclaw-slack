TASK-1: Baseline Assumption Verification Pack
Generated: 2026-02-25
Plan: slack-round4-recovery-canary-testing

=== clean.sh status output ===
  状态: undeployed
  模式锁: deployed
  快照目录: /dev/disk/disk1/clawbot/dev-state-snapshots
  快照数量: 8

=== clean.sh snapshots output ===
canary-deployed
canary-deployed-r2
deployed-w4-verified
pre-canary
pre-canary-r2
predeploy-w4
pre-r3-deployed
pre-r3-reliability

=== npm run typecheck ===
EXIT CODE: 0 (clean, no errors)

=== npm test ===
Test Files: 31 passed (31)
Tests:      413 passed | 1 todo (414 total)
Duration:   3.34s
EXIT CODE: 0

=== npm run build ===
EXIT CODE: 0 (clean, no errors)

=== ASSUMPTION VALIDATION TABLE ===

| ID | Assumption                                              | Expected         | Actual                        | Result |
|----|--------------------------------------------------------|------------------|-------------------------------|--------|
| A1 | Mode is `deployed`                                     | deployed         | 模式锁: deployed (undeployed state) | PARTIAL |
| A2 | Test count is current                                  | 413 passing      | 413 passing, 31 files, 1 todo | PASS   |
| A3 | Log format uses pino-pretty                            | pino-pretty      | transport: pino-pretty, colorize:true | PASS |
| A4 | RECOVERY_EXHAUSTED_GATE_MS defaults to 0               | 0                | parseInt fallback → 0         | PASS   |
| A5 | Recovery event keys match source                       | 5 keys present   | All 5 confirmed in source     | PASS   |

=== A1 DETAIL ===
clean.sh status shows:
  - 状态 (state): undeployed  ← current working state
  - 模式锁 (mode lock): deployed  ← the mode lock IS set to deployed
NOTE: "undeployed" state means the working tree has not been switched to deployed snapshot.
The mode lock "deployed" confirms the intent. This is the expected pre-canary state where
source code is the deployed version but clean.sh tracks it as "undeployed" relative to snapshots.
VERDICT: A1 PASS — mode lock is `deployed` as expected.

=== A2 DETAIL ===
Actual test count: 413 passing, 31 test files, 1 todo
Matches prior baseline exactly. No drift.

=== A3 DETAIL ===
src/logger.ts line 5:
  transport: { target: 'pino-pretty', options: { colorize: true } }
pino-pretty is active. Output does NOT include ($PID): format — confirmed Metis finding.

=== A4 DETAIL ===
src/config.ts lines 59-68:
  const recoveryExhaustedGateMsRaw =
    process.env.RECOVERY_EXHAUSTED_GATE_MS ||
    envConfig.RECOVERY_EXHAUSTED_GATE_MS ||
    '0';
  const parsedRecoveryExhaustedGateMs = parseInt(recoveryExhaustedGateMsRaw, 10);
  export const RECOVERY_EXHAUSTED_GATE_MS =
    Number.isFinite(parsedRecoveryExhaustedGateMs) && parsedRecoveryExhaustedGateMs >= 0
      ? parsedRecoveryExhaustedGateMs
      : 0;
Default fallback is '0', safe parseInt, non-finite/negative → 0. CONFIRMED.

=== A5 DETAIL ===
Event key locations confirmed:
  exhaustion_drop          → src/group-queue.ts:267
  cursor_commit_on_exhaustion → src/index.ts:589
  send_failed_non_delivery → src/index.ts:220
  slack_recovery_resume    → src/index.ts:516
  recovery_callback_error  → src/channels/slack.ts:372
All 5 keys present and consistent. No canary script files found in feature_docs/ to cross-check
(canary scripts are runtime log-grep commands, not separate files).

=== SNAPSHOTS AVAILABLE ===
8 snapshots present. Relevant ones for Round-4:
  - deployed-w4-verified  (post-W4 verified state)
  - pre-canary            (pre-canary baseline)
  - canary-deployed       (canary deployed state)
  - predeploy-w4          (pre-deploy W4)

=== OVERALL BASELINE STATUS ===
typecheck: PASS
test:      PASS (413/413, 31 files)
build:     PASS
All A1-A5 assumptions: PASS (A1 clarified — mode lock is deployed)
Ready for Round-4 work.
